using System.Collections;
using System.Collections.Generic;
using Supyrb;
using UnityEngine;

public class WaveStartedSignal : Signal
{
    protected override void Invoke(int index)
    {
        base.Invoke(index);
    }
}
